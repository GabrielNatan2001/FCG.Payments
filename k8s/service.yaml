apiVersion: v1
kind: Service
metadata:
  name: fcg-payments-worker
spec:
  selector:
    app: fcg-payments-worker
  
  ports:
   - protocol: TCP
     port: 80
     targetPort: 8080
  
  type: ClusterIP